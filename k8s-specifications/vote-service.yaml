apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
spec:
  type: ClusterIP
  ports:
  - name: "vote-service"
    port: 3000
    targetPort: 3000
  selector:
    app: vote
  
